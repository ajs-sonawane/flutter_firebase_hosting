import 'package:flutter/material.dart';

class BarsScreen extends StatelessWidget {
  const BarsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("BARS")),
    );
  }
}
