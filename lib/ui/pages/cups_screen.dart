import 'package:flutter/material.dart';

class CupsScreen extends StatelessWidget {
  const CupsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("CUPS")),
    );
  }
}
